server:
  port: 8080
  error:
    whitelabel:
      enabled: true
spring:
  security:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: false
#    properties:
#      hibernate:
#        format_sql: true
#        show_sql: true
    hibernate:
      ddl-auto: update